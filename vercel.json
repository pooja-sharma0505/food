{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "web-build",
  "devCommand": "npx expo start",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path((?!api/).*)",
      "destination": "/"
    }
  ]
}
